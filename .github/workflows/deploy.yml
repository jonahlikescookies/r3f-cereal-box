name: Deploy site
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.7
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run build
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: ls
